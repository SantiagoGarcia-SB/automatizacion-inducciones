{
  "name": "automatizacion-inducciones-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test infrastructure for Automatización de Inducciones (Google Apps Script project)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:properties": "vitest run tests/properties/",
    "test:unit": "vitest run tests/unit/",
    "test:integration": "vitest run tests/integration/"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "vitest": "^2.1.0"
  }
}
